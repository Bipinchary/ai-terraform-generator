# ============================================================
# Auto-generated by AI-Terraform Generator
# DO NOT EDIT MANUALLY
# ============================================================

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = "{{ region }}"
}

{% if ec2_instances %}
# -- AMI Lookup -------------------------------------------------------
data "aws_ami" "latest_amazon_linux" {
  most_recent = true
  owners      = ["amazon"]

  filter {
    name   = "name"
    values = ["al2023-ami-2023*-x86_64"]
  }
}

{% endif %}
{% if vpc %}
# -- VPC --------------------------------------------------------------
resource "aws_vpc" "main" {
  cidr_block           = "{{ vpc_cidr }}"
  enable_dns_support   = true
  enable_dns_hostnames = true

  tags = {
    Name = "{{ project_name }}-vpc"
  }
}

resource "aws_internet_gateway" "main" {
  vpc_id = aws_vpc.main.id

  tags = {
    Name = "{{ project_name }}-igw"
  }
}

resource "aws_route_table" "public" {
  vpc_id = aws_vpc.main.id

  route {
    cidr_block = "0.0.0.0/0"
    gateway_id = aws_internet_gateway.main.id
  }

  tags = {
    Name = "{{ project_name }}-rt-public"
  }
}

{% endif %}
{% if subnets %}
# -- Subnets ----------------------------------------------------------
{% for subnet in subnets %}
resource "aws_subnet" "public{{ subnet.index }}" {
  vpc_id                  = aws_vpc.main.id
  cidr_block              = "{{ subnet.cidr }}"
  availability_zone       = "{{ region }}{{ subnet.az_suffix }}"
  map_public_ip_on_launch = true

  tags = {
    Name = "{{ project_name }}-subnet-public{{ subnet.index }}"
  }
}

resource "aws_route_table_association" "public{{ subnet.index }}" {
  subnet_id      = aws_subnet.public{{ subnet.index }}.id
  route_table_id = aws_route_table.public.id
}

{% endfor %}
{% endif %}
{% if ec2_instances %}
# -- Security Group ---------------------------------------------------
resource "aws_security_group" "ec2" {
  name        = "{{ project_name }}-ec2-sg"
  description = "Security group for EC2 instances"
  vpc_id      = aws_vpc.main.id

  ingress {
    from_port   = 22
    to_port     = 22
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
    description = "SSH"
  }

  egress {
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }

  tags = {
    Name = "{{ project_name }}-ec2-sg"
  }
}

# -- EC2 Instances ----------------------------------------------------
{% for instance in ec2_instances %}
resource "aws_instance" "web{{ instance.index }}" {
  ami                    = data.aws_ami.latest_amazon_linux.id
  instance_type          = "{{ instance_type }}"
  subnet_id              = aws_subnet.public{{ instance.subnet_index }}.id
  vpc_security_group_ids = [aws_security_group.ec2.id]

  tags = {
    Name = "{{ project_name }}-ec2-{{ instance.index }}"
  }
}

{% endfor %}
{% endif %}
# -- Outputs ----------------------------------------------------------
{% if vpc %}
output "vpc_id" {
  description = "ID of the created VPC"
  value       = aws_vpc.main.id
}

{% endif %}
{% for subnet in subnets %}
output "subnet_public{{ subnet.index }}_id" {
  description = "ID of public subnet {{ subnet.index }}"
  value       = aws_subnet.public{{ subnet.index }}.id
}

{% endfor %}
{% for instance in ec2_instances %}
output "ec2_web{{ instance.index }}_public_ip" {
  description = "Public IP of EC2 instance {{ instance.index }}"
  value       = aws_instance.web{{ instance.index }}.public_ip
}

{% endfor %}
